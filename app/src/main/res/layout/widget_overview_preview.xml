<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/widget_background"
    android:paddingHorizontal="6dp">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_overview_text"
        android:textSize="16sp"
        android:textAlignment="center"
        android:textColor="@color/widget_onBackground"
        android:layout_marginBottom="6dp"/>

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/widget_overview_previewValue"
        android:textSize="21sp"
        android:textAlignment="center"
        android:paddingHorizontal="12dp"
        android:paddingVertical="4dp"
        android:layout_gravity="center"
        android:textColor="@color/widget_onPrimaryContainer"
        android:background="@drawable/background_value"/>

</LinearLayout>
